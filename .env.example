# Vercel: add these in Project → Settings → Environment Variables (Production)

VITE_API_URL=https://nut-backend-production-73f0.up.railway.app/api/v1
VITE_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# Local dev uses .env.development automatically with `npm run dev`
